name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: Dev/extension/pi-memorius/package-lock.json

      - name: Install dependencies
        working-directory: Dev/extension/pi-memorius
        run: npm ci

      - name: TypeScript check
        working-directory: Dev/extension/pi-memorius
        run: npm run typecheck

      - name: Lint
        working-directory: Dev/extension/pi-memorius
        run: npm run lint

      - name: Unit tests
        working-directory: Dev/extension/pi-memorius
        run: npm test
